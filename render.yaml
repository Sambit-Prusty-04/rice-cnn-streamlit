services:
  - type: web
    name: rice-cnn-streamlit
    env: python
    plan: free   # or standard, depending on your Render plan
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run app.py"
    pythonVersion: 3.11.9
